# Conduit-Container
This project containerizes a fullstack web application, consisting of a backend in django and the respective client in angular. The container setup can run using compose.yaml for easier management of containers.
